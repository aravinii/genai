# Python 3.10+. Use: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Agents & API
google-genai
python-dotenv
pydantic
fastapi
uvicorn

# Notebooks (render + kernel)
markdown
ipython
ipykernel

# Price model (house_price_model.joblib treinado com scikit-learn 1.4.2; manter versão para load sem warning)
pandas
scikit-learn==1.4.2
joblib
